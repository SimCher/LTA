<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:customControls="clr-namespace:LTA.Mobile.UI.CustomControls"
             xmlns:pageModels="clr-namespace:LTA.Mobile.PageModels;assembly=LTA.Mobile"
             x:DataType="pageModels:TestPageModel"
             x:Class="LTA.Mobile.Pages.Test.Test">
  <ContentPage.Content>
      <StackLayout>
          <Image x:Name="ImagePicker" Source="{Binding Image}" HeightRequest="300" WidthRequest="300" Aspect="Fill"/>
          <customControls:FloatingButton CornerRadius="90"
                                         HeightRequest="50"
                                         WidthRequest="50"
                                         Icon="{StaticResource FontAwesomePaperClip}"
                                         IconColor="{StaticResource PrimaryTextColor}"
                                         PressedCommand="{Binding PickPhotoCommand}"
                                         BackgroundColor="White"/>
      </StackLayout>
  </ContentPage.Content>
</ContentPage>